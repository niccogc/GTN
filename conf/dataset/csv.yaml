# @package _global_
defaults:
  - _base
  - size/small

hydra:
  sweep:
    dir: outputs/${dataset.name}/${trainer.type}/${model.name}_rg${trainer.ridge}_init${model.init_strength}
  run:
    dir: outputs/${dataset.name}/${trainer.type}/${model.name}_rg${trainer.ridge}_init${model.init_strength}

dataset:
  name: csv
  task: regression
  csv_path: ???
