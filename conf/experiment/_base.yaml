# @package _global_
# Base experiment sweep parameters
# Shared across NTN and GTN experiments

hydra:
  sweeper:
    params:
      # === Model Architecture (shared) ===
      model.L: 4,3
      model.bond_dim: 12,8,4
      # model.output_site: null
      # model.init_strength: 0.1
      
      # === Seeds (shared) ===
      seed: 42,10090,32874,47311,47303
