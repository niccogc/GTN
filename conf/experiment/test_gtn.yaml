# @package _global_
# GTN test experiment - evaluates best configs on test set

defaults:
  - test_base
  - override /trainer: gtn

hydra:
  sweeper:
    params:
      # === Optimizer (GTN-specific) ===
      trainer.lr: 0.005
      trainer.ridge: 0.005
      trainer.optimizer: adamw
