# @package _global_
# Quick test sweep (minimal grid)
# Usage: python run.py +experiment=quick_test dataset=iris --multirun

hydra:
  sweeper:
    params:
      model.L: 2
      model.bond_dim: 2,4
      seed: 0
