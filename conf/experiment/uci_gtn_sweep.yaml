# @package _global_
# UCI GTN Grid Search

defaults:
  - _base
  - override /trainer: gtn

hydra:
  sweeper:
    params:
      # === Optimizer (GTN-specific) ===
      trainer.lr: 0.005
      trainer.ridge: 0.005
      trainer.optimizer: adamw
      # trainer.loss_fn: null
      
      # === Training ===
      # trainer.n_epochs: 1000
      
      # === Early Stopping ===
      # trainer.patience: 10
      # trainer.min_delta: 0.001
