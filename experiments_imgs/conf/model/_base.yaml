# @package _global_

model:
  name: ???
  L: 3
  n_patches: 4
  rank_pixel: 4
  rank_patch: 4
  init_strength: 0.01

hydra:
  sweep:
    subdir: np${model.n_patches}_L${model.L}_rp${model.rank_pixel}_rpa${model.rank_patch}_seed${seed}
