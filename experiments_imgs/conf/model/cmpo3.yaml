# @package _global_
defaults:
  - _base

model:
  name: CMPO3
  rank_channel: 3

hydra:
  sweep:
    subdir: np${model.n_patches}_L${model.L}_rp${model.rank_pixel}_rpa${model.rank_patch}_rpc${model.rank_channel}_seed${seed}

