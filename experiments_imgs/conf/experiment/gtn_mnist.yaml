# @package _global_
# UCI GTN Grid Search

defaults:
  - _base
  - override /trainer: gtn

hydra:
  sweeper:
    params:
      # === Optimizer (GTN-specific) ===
      trainer.lr: 0.005
      trainer.ridge: 0.005
      trainer.optimizer: adamw
