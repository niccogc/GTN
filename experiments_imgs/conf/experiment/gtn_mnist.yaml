# @package _global_
# UCI GTN Grid Search

defaults:
  - _base
  - override /trainer: gtn

hydra:
  sweeper:
    params:
      # === Optimizer (GTN-specific) ===
      trainer.lr: 0.005
      trainer.weight_decay: 0.005
      trainer.optimizer: adamw
