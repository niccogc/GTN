# @package _global_
# UCI GTN Grid Search

defaults:
  - _base
  - override /trainer: gtn

hydra:
  sweeper:
    params:
      model.L: 3
      model.rank_pixel: 8,12,16
      model.rank_patch: 3,5,7
