gtn_test_grid:
  model:
    type: "L" # Assuming 'L' refers to a type of model
    level: 3
  training:
    epochs: 200
  run_config:
    iterations: 1 # Explicitly state one iteration
    track_what_happens: true # As requested "track what happens"